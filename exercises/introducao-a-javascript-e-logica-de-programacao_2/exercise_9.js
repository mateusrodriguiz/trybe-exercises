/* Utilizando o array criado no exercício anterior imprima o resultado da divisão de cada um dos elementos por 2 . */

let score = [];

for (index = 0; index <= 25; index += 1) {
    score.push(index);
    console.log(score[index] / 2);
}